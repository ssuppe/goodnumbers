feat(worker): P3_T2 implement basic job processing logic and unit tests

This commit introduces the basic job processing logic for the worker and its corresponding unit tests.

Key changes include:
- Created `tests/unit/worker.test.ts` to define the expected behavior of the `processJournalJob` function. This test initially failed (RED state) as the worker logic was not yet fully implemented.
- Refactored `src/worker.ts` to export `processJournalJob` and include the core logic to update a journal's status to 'COMPLETE' in the database. A `NODE_ENV !== 'test'` guard was added to prevent the worker from starting during test runs.
- Ensured `tests/unit/worker.test.ts` now passes (GREEN state) by correctly mocking Prisma interactions using `jest.unstable_mockModule` and dynamically importing the worker logic.

This completes the TDD cycle for the worker's basic job processing, providing confidence that the worker can update journal statuses upon successful job completion.