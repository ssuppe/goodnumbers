The EventClusterCard (found in frontend/src/components/journal/EventClusterCard.tsx) correctly has a collapsing textarea (collapsed when empty, expanded when clicked on/typed in). However, the Journal page (found in frontend/src/pages/JournalPage.tsx) using a custom component called ContextualNotesArea (found in frontend/src/components/journal/ContextualNotesArea.tsx). I prefer the collapsing textarea. Can we get rid of this ContextualNotesArea? Or does it serve some other purpose?
