{
  "extends": "./tsconfig.json",
  "include": ["src/**/*", "tests/**/*", "vitest.config.ts", "vitest.setup.ts", "eslint.config.js"],
  "exclude": ["frontend/**/*"] // Explicitly exclude the frontend directory
}
